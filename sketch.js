function setup() {
  createCanvas(400, 400);
  background('pink');
  point(200, 200);        

}

function draw() {
  //ellipse(mouseX, mouseY, 10, 10);
  fill(200);
  textSize(40);
  textFont('Canterbury')
  text("error404", mouseX, mouseY);
  
}